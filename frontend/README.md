# Kanban Board

A single-board Kanban project management app built with Next.js.

## Getting started

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

## Tests

```bash
npm test                  # unit tests (Vitest)
npx playwright test       # e2e tests (Playwright)
```
